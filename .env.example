# ─────────────────────────────────────────────────────────────
# Labo-Flow — Shared environment variables
# Copy to .env and fill in real values. This file is consumed
# by dev.sh / setup-all.sh and propagated to each component.
# ─────────────────────────────────────────────────────────────

# ══ Unified entry ══
NGINX_PORT=3008
PUBLIC_BASE_URL=http://localhost:3008

# ══ Clawith (main platform) ══
CLAWITH_FRONTEND_PORT=3080
CLAWITH_BACKEND_PORT=8008
# JWT_SECRET_KEY is read by Clawith backend AND shared with RAGFlow/AIPPT
# for SSO. Generate with: python -c 'import secrets; print(secrets.token_urlsafe(48))'
JWT_SECRET_KEY=change-me-to-a-long-random-secret
JWT_ALGORITHM=HS256

# ══ RAGFlow (knowledge base) ══
RAGFLOW_PORT=8880
# ─── RAGFlow MCP (server-to-server, internal) ────────
# Dev mode: dev.sh launches RAGFlow MCP server at localhost:9382 directly.
# Docker mode: top-level docker-compose.yml overrides this to
#              http://ragflow-cpu:9382/mcp via clawith-backend.environment.
# Per-user API keys are configured per-Agent in Clawith UI (like Tavily).
RAGFLOW_MCP_URL=http://localhost:9382/mcp
RAGFLOW_MCP_PORT=9382
# Must match Clawith's SECRET_KEY — used for RAGFlow SSO
CLAWITH_JWT_SECRET_KEY=
# Docker Compose profiles for RAGFlow: selects doc engine + device
# Default: elasticsearch (doc engine) + cpu (inference device)
# Change to "infinity,cpu" for Infinity vector DB, or "elasticsearch,gpu" for GPU
COMPOSE_PROFILES=elasticsearch,cpu

# ══ AIPPT (AI PPT frontend) ══
AIPPT_PORT=5173
# When integrated with Clawith SSO, AIPPT will read this token secret
VITE_JWT_SECRET=${JWT_SECRET_KEY}
# Point AIPPT's CUSTOM LLM provider at Clawith's LLM proxy (Phase 4)
VITE_CUSTOM_LLM_URL=http://localhost:8008/api/v1/llm-proxy
VITE_CUSTOM_API_KEY=
VITE_CUSTOM_MODEL=gpt-4o-mini

# ══ Database (shared by Clawith) ══
DATABASE_URL=postgresql+asyncpg://clawith:clawith@localhost:5432/clawith?ssl=disable
